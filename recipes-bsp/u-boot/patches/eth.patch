---  a/net/eth-uclass.c	
+++  b/net/eth-uclass.c
@@ -620,8 +620,8 @@
 		eth_env_set_enetaddr_by_index("eth", dev_seq(dev),
 					      pdata->enetaddr);
 #else
-		printf("\nError: %s address not set.\n",
-		       dev->name);
+		//printf("\nError: %s address not set.\n",
+		//       dev->name);
 		return -EINVAL;
 #endif
 	}

